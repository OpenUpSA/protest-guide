import { withStyles } from '@material-ui/core';


const styles = {
  root: {
    margin: '0 auto', 
    maxWidth: '1000px', 
    padding: '0 20px',
  },

  sectionWrapper: {
    marginBottom: '3rem',
  },
}


export default withStyles(styles);
